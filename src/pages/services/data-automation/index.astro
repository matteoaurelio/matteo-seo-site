---
import GrowthServicePage from "../../../components/GrowthServicePage.astro";
import { getGrowthService } from "../../../data/growthServices";

const service = getGrowthService("data-automation");

if (!service) {
  throw new Error("Growth service not found: data-automation");
}
---

<GrowthServicePage service={service} />
